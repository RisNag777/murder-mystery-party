# Host dashboard password
HOST_PASSWORD=change-me

# Public app URL (used for guest share links in the host dashboard)
PUBLIC_APP_URL=https://murder-mystery-party.streamlit.app

# On Streamlit Community Cloud, set the same keys in App settings → Secrets (TOML).
